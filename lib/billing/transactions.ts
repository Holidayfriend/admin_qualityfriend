type EventRow = { id:string;providerEventId:string;payload:unknown;createdAt:Date;hotelTenantId:string|null;hotelTenant?:{hotelNameEn:string;companyName:string}|null };
export type Transaction={id:string;eventId:string;hotelTenantId:string|null;hotelName:string;companyName:string;paypalTransactionId:string;subscriptionId:string|null;payerEmail:string|null;amount:number;currency:string;occurredAt:Date;status:"COMPLETED"};
function object(value:unknown):Record<string,unknown>{return value&&typeof value==="object"&&!Array.isArray(value)?value as Record<string,unknown>:{}}
function string(value:unknown){return typeof value==="string"?value:null}
export function transactionFromEvent(event:EventRow):Transaction|null{const payload=object(event.payload),resource=object(payload.resource),amountObject=object(resource.amount),amountValue=string(amountObject.total)||string(amountObject.value),amount=amountValue?Number(amountValue):NaN;if(!Number.isFinite(amount))return null;const payer=object(resource.payer),payerInfo=object(payer.payer_info),subscriber=object(resource.subscriber),eventDate=string(payload.create_time),occurredAt=eventDate&&!Number.isNaN(Date.parse(eventDate))?new Date(eventDate):event.createdAt;return{id:event.id,eventId:event.providerEventId,hotelTenantId:event.hotelTenantId,hotelName:event.hotelTenant?.hotelNameEn||"Unknown hotel",companyName:event.hotelTenant?.companyName||"",paypalTransactionId:string(resource.id)||event.providerEventId,subscriptionId:string(resource.billing_agreement_id)||string(resource.billing_agreement_id),payerEmail:string(payerInfo.email)||string(subscriber.email_address),amount,currency:string(amountObject.currency)||string(amountObject.currency_code)||"EUR",occurredAt,status:"COMPLETED"}}
export function money(value:number,currency="EUR"){return new Intl.NumberFormat("en",{style:"currency",currency}).format(value)}
