import bcrypt from"bcryptjs";import{NextResponse}from"next/server";import{prisma}from"../../../lib/prisma";import{loginSession}from"../../../lib/auth/session";export async function POST(r:Request){const b=await r.json().catch(()=>null),email=typeof b?.email==="string"?b.email.trim().toLowerCase():"",password=typeof b?.password==="string"?b.password:"",a=await prisma.superAdmin.findUnique({where:{email}});if(!a||!a.isActive||!await bcrypt.compare(password,a.passwordHash))return NextResponse.json({error:"INVALID_CREDENTIALS"},{status:401});await prisma.superAdmin.update({where:{id:a.id},data:{lastLoginAt:new Date()}});await loginSession(a.id);return NextResponse.json({success:true})}
