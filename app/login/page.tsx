"use client";import Image from"next/image";import{useRouter}from"next/navigation";import{useState,type FormEvent}from"react";export default function Login(){const r=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[show,setShow]=useState(false),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function go(e:FormEvent){e.preventDefault();setBusy(true);const x=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});setBusy(false);if(!x.ok)return setError("Email or password is incorrect.");r.replace("/dashboard")}return <main className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-[var(--navy)] p-14 text-white lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3"><Image src="/logo-icon.png" alt="" width={48} height={48} className="rounded-xl bg-white p-2"/><b className="text-xl">QualityFriend</b></div><div><p className="text-xs font-bold text-[var(--accent)]">SUPER ADMIN PORTAL</p><h1 className="mt-4 text-4xl font-bold">Manage every hotel from one secure place.</h1></div></section><form onSubmit={go} className="m-auto w-full max-w-md p-7"><h2 className="text-3xl font-bold">Admin sign in</h2><p className="mt-2 text-sm text-[var(--muted)]">Use your super-admin account.</p>{error&&<p className="mt-5 rounded bg-red-50 p-3 text-xs text-red-700">{error}</p>}<label className="mt-6 block text-sm font-semibold">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="mt-2 h-11 w-full rounded-lg border px-3"/></label><label className="mt-4 block text-sm font-semibold">Password<span className="relative mt-2 block"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?"text":"password"} className="h-11 w-full rounded-lg border px-3 pr-12"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-0 h-11 w-11">◎</button></span></label><button disabled={busy} className="mt-6 h-11 w-full rounded-lg bg-[var(--accent)] font-bold text-white">{busy?"Signing in…":"Sign in"}</button></form></main>}
